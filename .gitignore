################################################################################
# .gitignore
#
# This file tells Git which files and directories to ignore when committing
# changes. It keeps development clutter (dependencies, logs, temporary files)
# out of version control so only meaningful source code is tracked.
#
# The patterns below are grouped by category for clarity.
################################################################################

# ----------------------------------------------------------------------
# Node.js dependencies
# ----------------------------------------------------------------------
# Ignore all installed packages. These are restored via `npm install`.
node_modules/

# Common lockfiles and debug logs generated by npm, Yarn, or pnpm.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json

# ----------------------------------------------------------------------
# Logs
# ----------------------------------------------------------------------
# Generic log files created by scripts or daemons.
logs
*.log
*.log.*

# ----------------------------------------------------------------------
# Runtime data
# ----------------------------------------------------------------------
# Temporary PID files and lock files sometimes generated during process startup.
pids
*.pid
*.pid.lock

# ----------------------------------------------------------------------
# Environment configuration
# ----------------------------------------------------------------------
# Ignore local .env files containing credentials or secrets.
.env
.env.local
.env.*.local

# ----------------------------------------------------------------------
# OS / Editor-specific junk
# ----------------------------------------------------------------------
# macOS and Windows system artifacts, IDE configs, swap files, etc.
.DS_Store
Thumbs.db
.vscode/
.vscode/settings.json
.vscode/tasks.json
.idea/
*.swp
*.swo
*~

# ----------------------------------------------------------------------
# Build / Distribution / Temporary output
# ----------------------------------------------------------------------
# Ignore build artifacts, output folders, and cache directories.
dist/
build/
tmp/
temp/
.cache/

# ----------------------------------------------------------------------
# macOS system metadata
# ----------------------------------------------------------------------
# Files automatically created by Finder and compressed zips.
.AppleDouble
.LSOverride
Icon?
__MACOSX/

# ----------------------------------------------------------------------
# Matter / Commissioning data
# ----------------------------------------------------------------------
# Local Matter fabric, storage, or commissioning state.
# These files should remain local for security reasons and must not be shared.
*.fabric.json
*.commissioning.json
*.storage.json

# ----------------------------------------------------------------------
# Test coverage data
# ----------------------------------------------------------------------
# Optional: generated by testing or instrumentation tools.
coverage/
.nyc_output/

# ----------------------------------------------------------------------
# NPM packaging artifacts
# ----------------------------------------------------------------------
# Files created when packing or publishing the package to npm.
.npmignore
*.tgz
